version: "2"

linters:
  settings:
    errcheck:
      exclude-functions:
        # Usage text and diagnostics: if writing them fails there is nothing
        # left to report the failure with.
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln

formatters:
  enable:
    - gofmt
